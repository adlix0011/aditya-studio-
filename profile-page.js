let profileEditing=false;
let addressEditing=false;

function profileDistrictOptions(selected){
 const rows=typeof cgDistricts!=='undefined'&&Array.isArray(cgDistricts)?cgDistricts:['जांजगीर-चांपा'];
 return rows.map(d=>`<option value="${esc(d)}" ${d===selected?'selected':''}>${esc(d)}</option>`).join('');
}
function profileAddressText(u){return [u.address,u.landmark,u.village,u.district,u.pincode?('PIN '+u.pincode):''].filter(Boolean).join(', ')}
function profileAddressForm(u){return `<form id="addressForm" class="address-form"><label>घर नंबर / गली / मोहल्ला *<textarea name="address" required maxlength="250" rows="2" placeholder="घर नंबर, गली, मोहल्ला" autocomplete="street-address">${esc(u.address||'')}</textarea></label><label>गांव / शहर *<input name="village" required maxlength="100" value="${esc(u.village||'')}" placeholder="अपना गांव या शहर लिखें" autocomplete="address-level2"></label><label>जिला *<select name="district" required>${profileDistrictOptions(u.district||'जांजगीर-चांपा')}</select></label><label>पिन कोड *<input name="pincode" required inputmode="numeric" pattern="[0-9]{6}" maxlength="6" value="${esc(u.pincode||'')}" placeholder="6 अंकों का पिन कोड" autocomplete="postal-code"></label><label>पास की पहचान / Landmark <input name="landmark" maxlength="150" value="${esc(u.landmark||'')}" placeholder="जैसे: स्कूल, मंदिर या दुकान"></label><button class="btn" type="submit">✓ पता सुरक्षित करें</button></form>`}
function profilePage(){
 const u=state.users[user],orders=state.orders.filter(mine),completed=orders.filter(o=>o.status==='completed').length,address=profileAddressText(u);
 return `<section class="user-profile"><button class="back" data-go="home">← होम पर वापस जाएं</button><div class="profile-heading"><div class="profile-portrait" aria-label="यूज़र की प्रोफ़ाइल">${esc((u.name||'U').trim().charAt(0).toUpperCase())}</div><h1>मेरी प्रोफ़ाइल</h1><p>आपकी जानकारी और आपके सभी ऑर्डर</p></div><div class="profile-details"><div class="profile-field name-field"><span class="profile-glyph">♙</span><div><small>पूरा नाम</small><strong>${esc(u.name)}</strong></div></div><div class="profile-field phone-field"><span class="profile-glyph">☎</span><div><small>मोबाइल नंबर</small><strong>${esc(u.phone||'मोबाइल नंबर जोड़ें')}</strong></div></div><div class="profile-field village-field"><span class="profile-glyph">⌖</span><div><small>गांव / शहर</small><strong>${esc(u.village||'अपने गांव का नाम जोड़ें')}</strong></div></div><button class="profile-field address-field" id="editAddress" type="button"><span class="profile-glyph">⌂</span><div><small>मेरा पता</small><strong>${esc(address||'अपना Address जोड़ें')}</strong><em>${address?'✎ पता बदलें':'पूरा पता जोड़ने के लिए click करें →'}</em></div></button>${addressEditing?profileAddressForm(u):''}<label class="profile-language"><span>◉</span><div><small>ऐप की भाषा</small><select id="appLanguage"><option value="hi" ${window.appLanguage!=='en'?'selected':''}>हिंदी</option><option value="en" ${window.appLanguage==='en'?'selected':''}>English</option></select></div></label><button class="profile-edit" id="editProfile">✎ ${profileEditing?'बदलाव बंद करें':'जानकारी बदलें'}</button>${profileEditing?`<form id="profileForm"><label>पूरा नाम<input name="name" required maxlength="80" value="${esc(u.name)}" autocomplete="name"></label><label>मोबाइल नंबर<input name="phone" type="tel" inputmode="numeric" pattern="[6-9][0-9]{9}" maxlength="10" required placeholder="10 अंकों का मोबाइल नंबर" value="${esc(u.phone||'')}" autocomplete="tel-national"></label><label>गांव / शहर<input name="village" required maxlength="100" placeholder="अपने गांव का नाम" value="${esc(u.village||'')}" autocomplete="address-level2"></label><button class="btn" type="submit">जानकारी सेव करें</button></form>`:''}</div><section class="profile-orders"><div class="row"><h2>▤ मेरे ऑर्डर</h2><button class="profile-edit" data-go="orders">सभी देखें →</button></div><div class="profile-order-stats"><div><strong>${orders.length}</strong><span>कुल ऑर्डर</span></div><div><strong>${orders.filter(o=>!['completed','cancelled'].includes(o.status)).length}</strong><span>चालू ऑर्डर</span></div><div><strong>${completed}</strong><span>पूरे हुए</span></div></div>${orders.slice(0,3).map(o=>`<button class="profile-order" data-open="${o.id}"><span><strong>${esc(o.title)}</strong><small>${o.owner===user?'आपकी मांग':'आपने स्वीकार किया'} · ${money(total(o))}</small></span>${badge(o)}</button>`).join('')||'<p class="muted">अभी आपका कोई ऑर्डर नहीं है।</p>'}</section><button class="profile-logout" id="logoutProfile">↪ &nbsp; लॉग आउट</button><p class="profile-demo-note">प्रोफ़ाइल की जानकारी सुरक्षित सेव रहती है।</p></section>`;
}
const renderBeforeProfile=render;
render=function(){renderBeforeProfile();if(tab==='profile'&&!active)document.querySelector('.content').innerHTML=profilePage()};
document.addEventListener('click',e=>{const b=e.target.closest('button');if(!b)return;if(b.id==='editProfile'){profileEditing=!profileEditing;render()}if(b.id==='editAddress'){addressEditing=!addressEditing;render()}if(b.id==='logoutProfile'){profileEditing=false;addressEditing=false;active=null;try{['aditya_studio_session_v1','aditya_studio_persistent_login_v2','aditya_studio_wallet_snapshot_v1'].forEach(k=>localStorage.removeItem(k));sessionStorage.removeItem('kaamsetu-logged-out');sessionStorage.removeItem('local_delivery_view_v1')}catch{}window.location.replace('/?auth=login');return}});
document.addEventListener('submit',async e=>{
 if(e.target.id==='profileForm'){e.preventDefault();const d=new FormData(e.target),name=String(d.get('name')||'').trim(),phone=String(d.get('phone')||'').trim(),village=String(d.get('village')||'').trim();if(!name||!village||!/^[6-9][0-9]{9}$/.test(phone)){toast('नाम, गांव और सही 10 अंकों का मोबाइल नंबर भरें।');return}Object.assign(state.users[user],{name,phone,village});try{localStorage.setItem(KEY,JSON.stringify(state))}catch{toast('जानकारी सेव नहीं हुई।');return}profileEditing=false;render();toast('आपकी प्रोफ़ाइल सेव हो गई।');return}
 if(e.target.id!=='addressForm')return;
 e.preventDefault();const d=new FormData(e.target),address=String(d.get('address')||'').trim(),village=String(d.get('village')||'').trim(),district=String(d.get('district')||'').trim(),pincode=String(d.get('pincode')||'').trim(),landmark=String(d.get('landmark')||'').trim();
 if(!address||!village||!district||!/^[0-9]{6}$/.test(pincode)){toast('घर का पता, गांव/शहर, जिला और 6 अंकों का पिन कोड भरें।');return}
 const raw=localStorage.getItem('aditya_studio_session_v1')||localStorage.getItem('aditya_studio_persistent_login_v2');let session=null;try{session=raw?JSON.parse(raw):null}catch{}
 if(!session?.sessionToken){toast('पता save करने के लिए login करें।');return}
 try{const r=await fetch('/api/profile/address',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({sessionToken:session.sessionToken,address,village,district,pincode,landmark})}),out=await r.json();if(!r.ok||!out.ok)throw Error(out.message||'पता save नहीं हुआ');Object.assign(state.users[user],{address,village,district,pincode,landmark});localStorage.setItem(KEY,JSON.stringify(state));localStorage.setItem('local_delivery_address_'+session.mobile,JSON.stringify({address,village,district,pincode,landmark}));addressEditing=false;render();toast('✓ आपका पता सुरक्षित हो गया।')}catch(err){toast(err.message||'पता save नहीं हुआ। फिर कोशिश करें।')}
});
render();
