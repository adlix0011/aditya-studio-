const adminKey='kaamsetu-demo-v2';
const adminMoney=n=>'₹'+Number(n||0).toLocaleString('en-IN');
const safe=s=>String(s??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','>':'&gt;','<':'&lt;','"':'&quot;',"'":'&#39;'}[c]));
function readAdminState(){try{return JSON.parse(localStorage.getItem(adminKey))||{orders:[]}}catch{return{orders:[]}}}
function adminRender(){const state=readAdminState(),orders=state.orders||[],active=orders.filter(o=>['open','chat','booked','delivering'].includes(o.status)),locked=orders.reduce((n,o)=>n+Number(o.customerHold||0)+Number(o.helperHold||0),0);document.getElementById('adminStats').innerHTML=`<div><strong>${orders.length}</strong><span>कुल ऑर्डर</span></div><div><strong>${active.length}</strong><span>चालू ऑर्डर</span></div><div><strong>${adminMoney(locked)}</strong><span>Locked रकम</span></div>`;document.getElementById('adminOrders').innerHTML=orders.length?orders.map(o=>`<article class="admin-order"><div><b>${safe(o.title)}</b><small>${safe(o.area||'Location नहीं दी गई')} → ${safe(o.deliveryVillage||'')}</small></div><span class="status ${safe(o.status)}">${safe(o.status)}</span><p>Customer hold: ${adminMoney(o.customerHold)} · Helper hold: ${adminMoney(o.helperHold)} · Total: ${adminMoney(Number(o.items||0)+Number(o.fee||0))}</p></article>`).join(''):'<p class="empty">अभी कोई local order नहीं है।</p>'}
document.getElementById('refreshAdmin').addEventListener('click',adminRender);adminRender();
