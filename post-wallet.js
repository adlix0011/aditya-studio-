/* The post recharge decision is owned by paid-product-policy.js.
   Keep this compatibility function for older edit-order.js calls. */
function showPostMoneyNeed(form){ window.showPaidPostMoneyNeed?.(form); }
